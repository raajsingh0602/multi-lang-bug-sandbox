[build-system]
requires = ["setuptools>=68.0"]
build-backend = "setuptools.backends._legacy:_Backend"

[project]
name = "multi-lang-bug-sandbox"
version = "1.0.0"
requires-python = ">=3.10"
dependencies = ["pytest>=7.0"]

[tool.pytest.ini_options]
testpaths = ["tests"]
